---
'@eigenpal/docx-js-editor': patch
---

Add `Show / Hide document outline` to the command palette so
⌘⇧P → Enter (after a recent pick) toggles the outline with one
keystroke. Label flips with state.
